apiVersion: apps/v1
kind: Deployment
metadata:
  name: hr-graphql-api
  labels:
    app: hr-graphql-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hr-graphql-api
  template:
    metadata:
      labels:
        app: hr-graphql-api
    spec:
      containers:
      - name: hr-graphql-api
        image: us-east1-docker.pkg.dev/newwwbi-playground/newwwbi-registry/hr-graphql-api:1.0
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
          name: http
          protocol: TCP
        env:
        - name: APP_NAME
          value: "hr-graphql-api"
        - name: APP_ENV
          value: "prod"
        - name: PORT
          value: "3000"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: hr-graphql-api-secrets
              key: database-url
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"